# Vibra local dev

Two folders:
- `vibraCore` - Node.js backend
- `vibraUi` - React frontend (Vite)

Run backend:

```powershell
cd vibraCore
npm install
npm run dev
```

Run frontend:

```powershell
cd vibraUi
npm install
npm run dev
```
